<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <View
        android:id="@+id/placeholder_image"
        android:layout_width="@dimen/placeholderImageWidth"
        android:layout_height="@dimen/placeholderImageHeight"
        android:layout_margin="16dp"
        android:background="@color/light_gray"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/placeholder_title"
        android:layout_width="0dp"
        android:layout_height="@dimen/placeholderTextHeight"
        android:layout_marginTop="8dp"
        android:background="@color/light_gray"
        app:layout_constraintEnd_toEndOf="@+id/placeholder_image"
        app:layout_constraintStart_toStartOf="@+id/placeholder_image"
        app:layout_constraintTop_toBottomOf="@+id/placeholder_image" />

    <View
        android:id="@+id/placeholder_score"
        android:layout_width="100dp"
        android:layout_height="@dimen/placeholderTextHeight"
        android:layout_marginTop="4dp"
        android:background="@color/light_gray"
        app:layout_constraintStart_toStartOf="@+id/placeholder_image"
        app:layout_constraintTop_toBottomOf="@+id/placeholder_title" />

    <View
        android:id="@+id/placeholder_episodes"
        android:layout_width="100dp"
        android:layout_height="@dimen/placeholderTextHeight"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="16dp"
        android:background="@color/light_gray"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@+id/placeholder_image"
        app:layout_constraintTop_toBottomOf="@+id/placeholder_score" />

</androidx.constraintlayout.widget.ConstraintLayout>